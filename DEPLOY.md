# GitHub Pages 部署指南

## 📋 部署方法

这个项目已经配置好 GitHub Pages 部署，有两种方式：

### 方法 1：使用 GitHub Actions（推荐，自动化）

1. **推送代码到 GitHub**
   ```bash
   git add .
   git commit -m "Initial commit"
   git remote add origin git@github.com:mzli112358/rus_test.git
   git push -u origin main
   ```

2. **启用 GitHub Pages**
   - 进入 GitHub 仓库页面
   - 点击 `Settings` → `Pages`
   - 在 `Source` 部分选择 `GitHub Actions`
   - 保存设置

3. **自动部署**
   - 每次推送到 `main` 分支会自动触发部署
   - 或者手动在 `Actions` 标签页触发工作流
   - 部署完成后访问：`https://mzli112358.github.io/rus_test/`

### 方法 2：手动部署（使用 gh-pages）

1. **安装 gh-pages**
   ```bash
   npm install --save-dev gh-pages cross-env
   ```

2. **构建并部署**
   ```bash
   npm run build:gh
   npm run deploy
   ```

3. **配置 GitHub Pages**
   - 进入仓库 `Settings` → `Pages`
   - 选择 `Deploy from a branch`
   - 分支选择 `gh-pages`，文件夹选择 `/ (root)`

## ⚠️ 重要注意事项

### 1. 音频文件大小限制

GitHub 有文件大小限制：
- 单个文件最大 100MB
- 仓库总大小建议不超过 1GB

**解决方案**：
- 如果音频文件太大，考虑：
  - 使用 Git LFS（大文件存储）
  - 将音频文件存储在 CDN（如 GitHub Releases、Cloudflare R2）
  - 压缩音频文件

### 2. 文件路径映射

GitHub Pages 部署后，媒体文件的路径需要调整。

**选项 A**：使用相对路径（推荐）
- 确保所有媒体文件都在 `public/media/` 目录
- 代码会自动处理路径映射

**选项 B**：使用外部 CDN
- 将音频文件上传到云存储
- 修改 `mediaConfig.ts` 中的路径为完整 URL

### 3. 基础路径配置

仓库名称是 `rus_test`，所以访问地址是：
- `https://mzli112358.github.io/rus_test/`

如果更改仓库名称，需要修改：
- `vite.config.ts` 中的 `base` 配置

## 🔧 本地测试部署

在部署前，可以本地预览构建结果：

```bash
npm run build:gh
npm run preview
```

访问 `http://localhost:4173/rus_test/` 查看效果。

## 📝 部署检查清单

- [ ] 代码已推送到 GitHub
- [ ] 已启用 GitHub Pages（使用 GitHub Actions）
- [ ] 媒体文件已处理（大小检查）
- [ ] `mediaConfig.ts` 中的路径正确
- [ ] 测试了构建后的页面

## 🐛 常见问题

### Q: 页面显示 404
A: 检查 `vite.config.ts` 中的 `base` 路径是否正确

### Q: 音频文件无法播放
A: 
- 检查文件路径是否正确
- 确认文件已提交到仓库
- 检查文件大小是否超过限制

### Q: 部署后样式丢失
A: 确保 `base` 路径配置正确，所有资源路径都会加上 base 前缀

## 🔗 相关链接

- GitHub Pages 文档：https://docs.github.com/en/pages
- Vite 部署指南：https://vitejs.dev/guide/static-deploy.html#github-pages

